import Home from '../components/Home';

const HomeRoute = () => (
  <section className="d-flex d-flex-column align-center">
    <div className="container">
      <Home />
    </div>
  </section>
);
export default HomeRoute;
